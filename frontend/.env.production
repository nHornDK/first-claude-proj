# Development — do NOT commit (gitignored)
# Uncomment to use local docker-compose API instead of production
#  docker compose --env-file .env --env-file .env.development up -d --force-recreate
VITE_API_URL=https://api.dendo.dk/api
